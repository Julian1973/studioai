# Morning handover — 2 September 2026

Julian — the whole restructure is finished front to back, and The Box Monsters is a real project
in the studio with Episode 1 loaded and waiting for your first fire. Everything below is in plain
English; the technical record is TICKET_PACK_002.md and CLAUDE.md rule 87.

## What you asked for, and what is true now

**"The software is the pipeline; the Bears and the Box Monsters are projects."** Done. The engine,
the studio and the chairs contain no show. Each show lives in its own folder under `projects/` with
its own bible, canon, laws, chairs' taste, assets, scripts and episodes. Crystal Bears runs exactly as
it did — proven byte-for-byte after every step (286 stored packages and prompts, identical). The
Box Monsters is the second project, built from your own files.

**Twelve tickets landed on the branch `t40/projects`, one commit each:**
T41–T47 (last night), then today T48–T51 (the last cast words out of code), T52–T56 (generic chairs,
no adapters, the project template + wizard), T57–T59 (Box Monsters scaffolded, an isolation proof,
switching productions in the studio), T60 (the docs) and T63 (the PC updater). T61/T62/T64 are
"later", as the ticket pack says.

## The Box Monsters — where it stands

`projects/the-box-monsters/` holds, from the project you and Codex built on the PC today:
- your show bible as the locked canon, your style law, the five scene locations, continuity;
- the cast: Patch, Rumble, Tilly, Nib and Jenny (with their reference images), plus Teacher,
  Classmate and the two environmental voices as stubs — exactly as your characters.json had them;
- Jenny's ElevenLabs voice; the four monsters have NO voice yet (see "before you fire");
- the Episode 1 script, "The Box That Felt Small", activated in the project's script store with the
  identical fingerprint to your upload (sha256 624e0080…);
- canon LOCKED — your 16:29 lock, re-based to the new layout (it says so in `lockedBy`);
- the Design tab roster (characters, locations, the four props) and your key art.

The studio boots on it cleanly (profile, projects, roster, episodes, canon lock, asset library all
answered). Preflight says Episode 1's next action is **"Approve and promote Story & Direction"** —
that is Gate 1, and it is your fire.

**Before you fire Gate 1 tomorrow**
1. Double-click **Start AI Studio** on the Desktop.
2. On Productions, open **The Box Monsters** — the studio switches itself to it.
3. Story & Direction on Episode 1 is the first paid step (the Director's LLM pass). Fire it.
4. Before any beat with dialogue can render, cast the four monsters' voices in the studio
   (Law 5: a beat with dialogue and no voice track refuses to render). Jenny already has hers.
5. Assets: only the five anchors (+ Jenny's turnaround) exist. Scene plates and wide keyframes are
   generated at Gate 2 as usual.

## The PC install — DONE (2 Sept, midday)

The studio now runs on the PC from **`C:\Users\julia\AiStudio`** (branch `t40/projects`, commit
807c662). Two Desktop icons: **Start AI Studio** (leave its black window open while you work) and
**Update AI Studio** (pulls in anything new I send). Open the studio in Chrome at
`http://127.0.0.1:8765/cb-studio/director.html`, then Productions → The Box Monsters.

Why it moved: the old copy at `C:\Users\julia\OneDrive\Desktop\Ai Studio` sits inside OneDrive, which
refuses to let git delete or replace folders — every checkout attempt there stalled on "Deletion of
directory … failed. Should I try again?". The updater (v9) therefore clones the repository locally to
`AiStudio`, fetches origin + the hand-delivered `studio-update.bundle`, checks out the branch, copies
`cb-seed`, `engine/media`, both projects' `assets`, the studio state files and `api.rtf` across
(never overwriting, never deleting), builds `.venv`, and writes the Desktop launchers. The OneDrive
copy is untouched and stays as a backup; its earlier stashes/`_local-work-*` folders are still there.

Three real Windows bugs were found on the first start and fixed on the branch the same hour:
`fcntl` (POSIX-only) → `engine/filelock_compat.py`; CRLF line endings breaking the script store's
SHA-256 checks → `.gitattributes` pins LF and the updater renormalises; two `UnboundLocalError`s in
`cb_intake.intake_status` / `cb_render.lineage_status` when the store refuses.

**Afternoon, 2 Sept — the first Gate-1 fire on the PC.** "Run Story & Direction" showed its green
toast and then nothing. Cause: every studio job was launched as the command `python3`, which on
Windows is the Microsoft Store stub ("Python was not found") — the Director's job died a second
after it started and its failure was hidden in the collapsed production checks. Fixed on the branch
(commit 5bf95e4): jobs launch with the studio's own `.venv` python; the live log reader no longer
uses `select()` (sockets-only on Windows); Stop kills the whole process tree with `taskkill`; the
studio's self-reload/project switch exits with code 75 and **Start AI Studio** relaunches it in the
same window; a failed run is now reported at the top of the Story & Direction panel with its reason.
The studio also runs itself in UTF-8 mode on Windows so the engine's own JSON and "—"/"✓" log lines
can never trip cp1252. Delivered as `studio-update.bundle` → Update AI Studio.

Still no GitHub push from this session (403). Publish from the PC when ready:
`git push -u origin t40/projects` (from `C:\Users\julia\AiStudio`), or add the repo to the session sources.

## Two things I did that you should know about

- **Canon re-locks.** Crystal Bears' canon lock is untouched and current (the chair files it hashes
  are reached through symlinks at their old paths). The Box Monsters' lock was rebuilt by me because
  the file paths changed with the new layout; the content is your 16:29 lock and `lockedBy` records
  that. If you'd rather it carry only your name, re-lock it in the studio — one click.
- **The chairs are split.** The craft of each chair (role, responsibility, workflow, the real-world
  influence, the runtime contract) is now the studio's, at `studio/chairs/<role>/SKILL.md`, and
  names no show. Your Crystal Bears chair documents moved, unchanged, to
  `projects/crystal-bears/chairs/`. The words a worker actually receives are byte-identical to
  before — proven for all six chairs.

## Still open (deliberately)

- T61: delete the compatibility links and re-lock Crystal Bears canon with the runtime chairs at
  their real paths — your action in the studio after T60 is signed.
- T62: renaming `cb-studio`/`cb_` — your call.
- T64: the test suite still writes into two real project files (the prompt bank and the asset
  registry); revert them after a run until it's fixed.
- The animation chair's SKILL.md still says "Julian" twice — it is hashed by the Crystal Bears lock,
  so it changes at the T61 re-lock, not before.

---

# Evening handover — 2 September 2026

Julian — the session crashed at about 18:36, mid Scene 2. Nothing was lost: everything that was
on disk is now committed, the studio is running, and the test suite is green for the first time
on this PC. Five commits on `t40/projects`, from `5307d62b` to `4282458e`.

## Where you actually are

The studio is up at **http://127.0.0.1:8765/cb-studio/app.html**, on The Box Monsters, and the
Director's board reads:

| Scene | What it wants from you |
|---|---|
| 1 | Fire Scene World (the Studio prepares Look direction itself) |
| 2 | **A finished keyframe is waiting for your decision** — S2.SH03, two candidates |
| 3 | Review the Story & Direction candidate: Approve or Iterate |
| 4 | Approve and promote Story & Direction |
| 5 | Review the Story & Direction candidate: Approve or Iterate |

Scenes 3, 4 and 5 have exactly one blocker each, and it is your decision in every case. Scene 2's
two keyframes (S2.SH03 A and B) were the last thing that rendered before the crash — 18:34 and
18:35, $0.19 the pair, one on Seedream and one on Gemini.

## What was in flight, and is now committed

**T71 — the delivery format, and the writer's own shot plan.** `engine/cb_format.py`: a project's
`profile.json` declares its `format` block (The Box Monsters: 30-second shots, 7 minutes, 15
shots, treatment style, audience 8-12), and a TREATMENT script's own `Shot NN:` headings become
the production units. The Creative Room's fast path skips the three whole-scene treatments and
the showrunner's selection when the writer's breakdown IS the treatment — which is what you were
asking about when you said "should it really be this long as it's a short script that is already
broken down." Crystal Bears is untouched: a project that declares no format keeps the full room.

Alongside it: scene-look generate/upload/select and approve/reject, keyframe approve/reject,
pose-reference rejection, voice reject and restore-previous-take, cb_layout's opening-frame
blocking (and `rembg`, which it needs — the Mac had it installed by hand), the department
workers, the storyboard-lineage requirement, `decide_intake`, canon status and re-lock, The Box
Monsters' identity packs, and Ep1's whole output as it stands.

## Five real bugs found and fixed, none of them yours

1. **A second, unread state database.** `projects/the-box-monsters/projects/the-box-monsters/…/
   studio.sqlite3` — the reference-control writers were handing cb_db the project root instead
   of the repository root, so it joined the project's own output path on twice. Written at
   18:31, five minutes before the crash. The control files themselves were fine; only their
   revision bookkeeping went astray, which means those writes had no conflict detection at all.
   Moved aside to `_archive_stray_state_db_2026-09-02/`, not deleted.
2. **The Director's board was down.** `/api/director-board` answered `name '_CBR' is not defined`
   for every episode with a package — that is every episode under way. Broken since long before
   today; it is what the board is now telling you above.
3. **Crystal Bears' canon would not load at all on this PC.** Every legacy `../cb-seed/…` asset
   path resolved one level above the workspace, because the test that recognises them checked
   for `"../"` and Windows renders it `"..\"`. Crystal Bears now reads current, zero blockers.
4. **ffmpeg was not installed.** Every scene was carrying a `CONFIG_FFMPEG` blocker. Installed
   (9.0.1, from the official Gyan build via winget) and every one of those blockers is gone.
5. **Text was being read as cp1252, not UTF-8.** 387 reads and writes across 67 files named no
   encoding, so any file holding an em dash raised on Windows. Same family as this morning's
   fcntl and CRLF fixes. Plus Windows long paths (an archived script version's name reached
   260 characters and the write failed), a transient file-lock retry, and the session secret,
   which on this machine was inheriting an ACL that gave a whole group write access — it is now
   yours alone.

## The tests

**979 passed, 4 skipped, nothing failing.** It was 72 failing before today's work. Plain
`pytest` from the repository root now just works, whichever production the studio is switched
to, and a full run leaves `git status` clean — **T64 is closed**.

## The one thing left for you

`git push -u origin t40/projects` — five commits are on this machine only. The push needs your
GitHub credentials, so it waits for you (GitHub Desktop is installed if that is easier).

Two smaller things, both yours by rule rather than by difficulty: The Box Monsters' four
monsters still have no ElevenLabs voice (Law 5 refuses to render a beat with dialogue and no
voice track), and T61 — deleting the compatibility links and re-locking Crystal Bears canon —
is still open and is a showrunner action, not mine.
