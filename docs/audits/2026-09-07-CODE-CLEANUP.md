# Code and documentation cleanup — 7 September 2026

The audit examined the active Python engine, local server, maintenance tools, current UI
API routes, current documentation and runtime department skills. It removed obsolete paths
rather than adding another production gate. No provider generation was performed.

## Fixed

- Removed 34 obsolete files from the active tree: superseded doctrines and roadmaps,
  unreferenced Studio wrapper instructions, old status notes and one-off maintenance or
  generation scripts. Recovery copies are inert files in the dated archive.
- Replaced the accumulated CLAUDE.md rule history with a concise agent entrypoint. Updated
  README.md and the current production contract to the script/budget/SEE/HEAR/WATCH sequence.
- Removed historical instructions from seven department skills. The five existing marked
  runtime role contracts remain text-identical. Removed an embedded NUL from the old
  cinematography document by retaining its clean current contract.
- New story direction receives the current production standard instead of the old Studio
  Bible's technical workflow. The locked Bible itself, its hashes and all canon records
  remain untouched. Character identity and show canon continue to come from locked sources.
- Removed missing-worker launchers for the old writer, relay and continuity paths. Script
  upload is the functioning entry point; the dead writer form and seed-generation controls
  have been removed.
- Removed the separate Anthropic room proxy. Retired writer/room endpoints return HTTP 410
  without starting jobs or making provider calls. Old room/board bookmarks redirect to the
  current workspace. Default Studio launch now opens app.html; director.html remains the
  supported advanced inspector.
- Removed the deleted-module import hidden inside the legacy post trim fallback, while
  preserving the historical trim value for old exports. Rebuilt field_audit.py against
  the current Shot/ContinuityState/CharacterState schemas. Its seven initial zero-hit
  candidates were traced to validation/emission consumers inside cb_engine itself; the
  audit now includes that mixed schema/compiler module.
- Removed a duplicate handover function declaration. The public transaction wrapper now
  calls a clearly named implementation; the scene lease and dry-run behaviour are preserved.
- Removed the remaining 9.5 emission-score production gate. Scores and FIX/POLISH findings
  are advisory. Concrete FATAL contract failures still block, as do the independent script,
  reference, voice, provider and authorization checks.
- Optional prompt readback now carries the episode budget context.
- Added a repeatable read-only source audit to catch missing local imports/workers, broken
  documentation links, malformed runtime boundaries, duplicate top-level definitions and
  browser API routes absent from the server.

## Evidence

- `2026-09-07-source-audit.json`: source audit and declared browser routes.
- `2026-09-07-code-cleanup-tests.txt`: 1,074 passed, 4 skipped; successful process exit.
  Final focused checks also passed: 47 handover/source-audit tests (1 skipped), and 11
  canon/source-audit tests after the final cleanup.
- `2026-09-07-code-cleanup-production.json`: read-only production audit.
- `../archive/2026-09-07-code-audit/MANIFEST.json`: 45 original source/document records with
  paths and SHA-256 hashes; all archive hashes were verified.

The eight Episode 2 scene lineages remain current. All 19 shot asset audit records match
the pre-cleanup audit, including existence and approval hash checks. No production package
was changed by the audit. Live health reported current code and zero running jobs, and
both retired page redirects were verified through the authenticated local server.

## Retained deliberately

Approved media, production state, locked canon, historical approvals, vendored provider
grammar, the supported advanced inspector and old lock-metadata readers remain. Historical
source archives are not loaded into runtime skills or served as application code. The
unrelated Node/Replit project and imported prototypes are outside this animation cleanup.

This is evidence of a code and documentation audit, not certification that every possible
runtime defect is absent. Episode 3's first complete live production run remains necessary
to verify the actual provider and review experience.
