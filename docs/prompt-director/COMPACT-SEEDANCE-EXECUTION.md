# Compact Seedance execution — 1.0

## Authorised live follow-up — 2026-09-10

Julian subsequently authorised locking the refined S1.SH2 direction and firing
one 26-second take. The final native request passed real SEE action readiness
and Prompt Director review, plus the 10/10 structural authoring check. BytePlus
accepted task `cgt-20260910215036-9nkxw`; it was running when recorded here.
No returned-film approval is implied. The actual transmitted prompt, all seven
image bodies and unchanged Audio1 bytes were verified against the sealed package.
Evidence: `cb-output/qualification/S1.SH2_compact_execution_v2/`.

The automatic specialist rewrite omitted some accepted visual timing detail.
The shared typed-edit route now optionally synchronises accepted visual fields
into their existing source coverage and Director Card, preserves view IDs,
visibility, timing, dialogue and media approvals, and refreshes dependent camera
evidence before saving the animation candidate. It records source history and
voids the old spend token. The 31 focused handoff/native/storyboard tests passed;
the actual S1.SH2 source-to-seal path then ran successfully. This qualifies that
route, not every editor or automatic interpretation of arbitrary feedback.

The sections below describe the earlier no-provider implementation pass.

Status: implemented locally and structurally tested. No live semantic review,
provider submission, server reload, migration, Git push or approval change in
this pass. The returned character swap is observed evidence; prompt overload
is a hypothesis, not a proven cause.

## Completed

- The shared Prompt Director compiles supported native timed-view prompts before
  reviewing them. The review receives full source direction, dynamic state,
  reference authority and role records separately. The final native envelope
  receives the compact result and retains the complete compilation evidence.
- The final order remains audience purpose, acting/camera views, reference/world
  truth, then protected audio/provider instructions. The prompt checker accepts
  the concise purpose/continuity headings without reducing its checks or threshold.
- Native subject selection and final visible-cast emission use declared view
  visibility. An offscreen reminder or offscreen speaker does not activate a
  visible character. Unknown legacy visibility is not guessed. Necessary joint
  actions and offscreen causes stay in the text.
- Repeated identity bibles and raw state/editorial records stay in internal
  evidence. Actual manifest tags bind visual character names. Canon traits are
  stated globally; each view has a short cast/role anchor. The global witness
  footer cannot accidentally become an instruction inside the final view.
- Opening, previous-ending and scene-plate references have limited authority.
  Previous frames carry explicitly surviving incoming state, not a replacement
  opening camera or later ownership. Movable state follows the directed sequence.
- Exact Dialogue Authority, Audio and AUDIO AND EXCLUSIONS blocks, plus all
  Spoken action lines, are preserved. Audio files, dialogue, cue timings and
  references are not rewritten. Post-review verification checks audio blocks and
  source binding again and rejects reintroduced control dumps.
- Configurable preferred range: 900–1,400 words. Warn above 1,600; block above
  2,200 unless an explicit authorisation record matches the shot/source/prompt.
  The compiler never truncates actions or dialogue to meet the limit. A budget
  block archives a reason, source/package hashes, references, corrective action
  and no-provider/no-spend evidence before a reviewer or generation call.
- 157 focused tests passed across the compact compiler, native sealing, Prompt
  Director, role integrity, dynamic state, handoff, Prompt Lab and WATCH evidence.
  Fixture review verdicts are injected and network is prohibited in route tests.

## S1.SH2 prepared candidate

`cb-output/qualification/S1.SH2_compact_execution_v1/` contains the proposed source,
visual-direction source, reproducible preparation script, compiler evidence,
exact proposed prompt, structural checks and test output.

The transmitted predecessor had 4,377 words. The prepared candidate has about
1,400, preserving 26 seconds, all six views, all four dialogue cues and the exact
Audio1 WAV SHA256 `7a2a0ab54db7ef6d782d8b775a49732bf89f5286b97830b0dca97cebfac021e4`.
The candidate coverage removes Zenny from the chase view's visible/critical entity
list; it retains her passive distant opening/grab role and isolated final wink.
The legacy coverage description and derived Director Card are aligned in this
candidate. None of those candidate changes are written over live approved work.

## Qualification boundary

Structured records and literal checks do not recognise faces, judge acting or
prove that Seedance will follow the story. The actual render still needs the
normal current-source, semantic Prompt Director, budget and human outcome path.
There is no new render or live Studio deployment to approve from this pass.

Compaction is qualified for the native timed-view format. Other prompt formats
retain their existing emission and receive the shared budget check; project JSON,
untimed legacy prompts and targeted source-video edits are not claimed to have
the same new compact formatting. No Studio-wide readiness claim is made.
